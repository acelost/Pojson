plugins {
    id 'java-library'
    id 'kotlin'
}

ext {
    PUBLISH_GROUP_ID = acelost_group_id
    PUBLISH_VERSION = pojson_library_version
    PUBLISH_ARTIFACT_ID = 'pojson-kotlin-collection'
}

apply from: "${rootProject.projectDir}/scripts/publish-module.gradle"

java {
    sourceCompatibility = JavaVersion.VERSION_11
    targetCompatibility = JavaVersion.VERSION_11
}

dependencies {
    api project(':pojson')
}